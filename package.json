{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "A TypeScript Todo application with CRUD operations",
  "scripts": {
    "start:json-server": "json-server --watch data/db.json --port 3000",
    "build": "tsc && parcel build index.html",
    "start": "parcel index.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "json-server": "^0.16.3"
  },
  "devDependencies": {
    "parcel": "^2.0.0",
    "process": "^0.11.10",
    "typescript": "^4.1.3"
  }
}
